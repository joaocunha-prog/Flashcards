{
  "name": "monthly-expenses-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for the monthly expenses tracker: REST API + WhatsApp Cloud API webhook",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
